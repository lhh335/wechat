<!--wealth.wxml-->
<view class="container log-list">
  <button bindtap='bindTaps'>{{'点击' + showText +  '我的位置'}}</button>
  <maps 
    id="my_map"
    bind:toggleMap='_toggleMap'
    longitude="{{location.longitude}}"
    latitude="{{location.latitude}}"
  />
</view>